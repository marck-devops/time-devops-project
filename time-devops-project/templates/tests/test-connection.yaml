apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "time-devops-project.fullname" . }}-test-connection"
  labels:
    {{- include "time-devops-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "time-devops-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
