apiVersion: v1
kind: Service
metadata:
  name: {{ include "time-devops-project.fullname" . }}
  labels:
    {{- include "time-devops-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "time-devops-project.selectorLabels" . | nindent 4 }}
